[package]
name = "guessing_game"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# 0.8.5事实上是^0.8.5的简写，他表示任何至少是0.8.5但是小于0.9.0的版本
rand = "0.8.5"
